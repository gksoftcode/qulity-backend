package com.wisecode.core.repositories;

import com.wisecode.core.entities.CheckListItemTransaction;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CheckListItemTransactionRepository extends JpaRepository<CheckListItemTransaction,Long> {
}
