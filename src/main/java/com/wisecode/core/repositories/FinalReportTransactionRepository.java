package com.wisecode.core.repositories;

import com.wisecode.core.entities.FinalReportTransaction;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FinalReportTransactionRepository extends JpaRepository<FinalReportTransaction,Long> {
}
